import * as React from 'react';
import { NextPage } from 'next';
import { Shell } from '../src/components';

const Home: NextPage = () => (
  <Shell>
    <p>Home</p>
  </Shell>
);

export default Home;
